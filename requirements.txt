# Core dependencies
loguru>=0.7.0
pydantic>=2.0.0
websockets>=12.0
httpx>=0.25.0
aiohttp>=3.9.0
rich>=13.0.0
click>=8.0.0
pyyaml>=6.0
filelock>=3.12.0

# Optional dependencies (uncomment as needed)
# openai>=1.0.0        # For OpenAI-compatible providers
# anthropic>=0.18.0    # For Anthropic provider
# ollama>=0.1.0        # For Ollama local models
